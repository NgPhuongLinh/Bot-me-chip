const login = require("./fb-chat-api");

login({ email: "nsjssjnsskskakk@gmail.com", password: "nguyenphuonglinh209" }, (err, api) => {
  if (err) return console.error(err);

  api.listenMqtt((err, message) => {
    if (err) return console.error(err);

    if (message.body === "•spam") {
      const spamText = "chippctee";
      const spamArray = Array(100).fill(spamText);
      spamArray.forEach((text) => {
        api.sendMessage(text, message.threadID);
      });
    }
  });
});
